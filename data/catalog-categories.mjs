// Bridge: re-export the existing frontend service categories so the backend
// seeder uses the exact same data (single source of truth, no duplication).
export { serviceCategories } from "../../tnh-salon/src/data/serviceCategories.js";
